# 作 者：田宗林
# 时 间：2021/8/5
"""
字符串的查询操作（r表示从右边查找）  ----> index
# str_name.index(substr_name)
    # 查找substr_name第一次出现的位置，如果不存在，抛出ValueError
# str_name.rindex(substr_name)
    # 查找substr_name最后一次出现的位置，如果不存在，抛出ValueError
# str_name.find(substr_name)
    # 查找substr_name第一次出现的位置，如果不存在，返回-1
# str_name.rfind(substr_name)
    # 查找substr_name最后一次出现的位置，如果不存在，返回-1
"""
