# 作 者：田宗林
# 时 间：2021/8/5
# 集合：没有value的字典，元素不允许重复（自动去重）且无序
# 集合的创建
    # s = {e1, e2, ...}
    # s = set{任意一种数据结构}，如：range、列表、元组、字符串、字典、集合
    # s = set()  # 创建一个空集合

s1 = set()
print(s1, type(s1))
s2 = set(range(5))
print(s2)
